const {
  GraphQLList,
  GraphQLObjectType,
  GraphQLSchema,
  GraphQLInt,
  GraphQLNonNull,
  GraphQLString,
} = require("graphql");

// TODO référencier les ressources "endpoint" dans Query

// TODO créer le schema graphQL
module.exports = {};